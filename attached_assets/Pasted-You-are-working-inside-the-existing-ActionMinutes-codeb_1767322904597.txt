You are working inside the existing ActionMinutes codebase (Vite + React/TS client, Express server, Drizzle ORM in shared/schema.ts, sessions table, Capacitor already present). Do NOT start a new project. Implement a cohesive “Glue + Release Polish” pass that fixes workflow gaps and makes the product feel unified.

========================
A) FIX ACTION STATUS INCONSISTENCY (PENDING -> OPEN)
========================
Problem: server AI mapping returns status "pending" but client types + inbox expect statuses: needs_review | open | waiting | done. This causes new actions to disappear from Inbox.

Changes:
1) Update server/ai/mapConfidenceToStatus() to return:
   - "needs_review" if confidenceOwner < 0.65 OR confidenceDueDate < 0.65
   - otherwise "open" (NOT "pending")
2) Add a one-time DB cleanup step:
   - Any action_items.status == 'pending' must be updated to 'open'
   - Implement as a small script in script/ or server startup guard (run once) and/or include a SQL note in replit.md.
3) Add server-side validation for action status updates:
   - only allow: needs_review, open, waiting, done
   - if unknown status is passed, return 400 with a friendly message.
4) Ensure client renders any unknown status as "open" defensively (badge + filters) to avoid regressions.

Acceptance:
- Extract meeting -> action items appear in Inbox under Open (unless needs_review).

========================
B) FIX reminderAt MISMATCH (UI WRITES FIELD THAT DB DOESN’T HAVE)
========================
Problem: client ActionEditSheet sends reminderAt but shared/schema.ts action_items table has no reminderAt. Drizzle update will fail or silently ignore, causing bugs.

Implement reminderAt properly:
1) Add column to shared/schema.ts actionItems table:
   - reminderAt: timestamp("reminder_at")
2) Ensure the Zod schemas / types exported from shared/schema include reminderAt.
3) Update server storage/updateActionItem typing so reminderAt is included.
4) Update client types (ActionItem interface/type) to include reminderAt?: Date | string | null.
5) Ensure ActionEditSheet reads/writes reminderAt end-to-end without errors.

DB:
- After code changes, run `npm run db:push` (Drizzle push) and ensure the column exists.

Acceptance:
- Setting reminderAt in ActionEditSheet persists after refresh.

========================
C) PERSIST ONBOARDING + SETTINGS (CURRENTLY ONLY LOCAL STORE)
========================
Problem: onboarding updates Zustand store but does not persist user preferences to server, so refresh loses settings.

Fix:
1) Add a client API helper for updating current user:
   - PATCH /api/users/me (already exists server-side)
2) Update Onboarding page to call PATCH /api/users/me with:
   - tone, autoGenerateDrafts, enablePersonal, timezone (if present), hasCompletedOnboarding=true
   - then update local store with returned user
3) Update Settings page to persist changes via PATCH /api/users/me instead of only local state.
4) Ensure useAuth hook sets store from /api/auth/user, and store does NOT override server truth.

Acceptance:
- Complete onboarding -> refresh -> still onboarded, preferences retained.

========================
D) WORK-FIRST COHESION: DEFAULT “MY WORK” WORKSPACE + BETTER WORKSPACE LOADING
========================
Goal: remove confusing “Personal = no workspace” as the default. Personal should be Journal/Reminders bonus; Work should be the core.

Implement:
1) On successful registration (server /api/auth/register):
   - Create a default workspace named "My Work"
   - Add the user as workspace owner
2) Add a server-side guard on login/auth user fetch:
   - If a user has zero workspaces, create "My Work" and membership owner (idempotent).
3) Update server /api/workspaces listing to return role too:
   - Return objects like { id, name, role } via join workspace_members.role
4) Update client useWorkspaces() to store real role instead of hardcoding member.
5) Update client default selection:
   - If currentWorkspaceId is null and workspaces exist, setCurrentWorkspace(workspaces[0].id)
6) Update WorkspaceSwitcher labeling:
   - If no workspace selected, show “Select workspace” (not “Personal”)
   - Personal features should live under separate navigation, not in workspace label.

Acceptance:
- New user registers -> lands with “My Work” selected and Inbox shows workspace actions.

========================
E) PERSONAL VS TEAM CLEAR SEPARATION (WITHOUT CONFUSING WORK FLOWS)
========================
Implement clean IA (information architecture):
1) Add a top-level nav section (in layout):
   - Work: Inbox, Meetings, Drafts, Agenda
   - Personal: Journal, Reminders (only visible if user.enablePersonal = true)
2) Ensure meetings/actions/drafts are always scoped to current workspace by default.
3) Personal pages must NOT depend on currentWorkspaceId.

Acceptance:
- Switching workspace affects Work screens only; Personal stays personal.

========================
F) CLOSE THE FOLLOW-UP LOOP (EXTRACTION -> DRAFTS -> WAITING/DONE)
========================
Right now drafts exist but the workflow isn’t “closed.”

Implement:
1) After extraction completes:
   - Show clear CTA: “Generate follow-up drafts”
   - If user.autoGenerateDrafts == true, automatically call POST /api/meetings/:id/generate-drafts
2) When a draft is created in Gmail/Outlook:
   - Update follow_up_drafts.state to "exported"
   - Automatically update related action items in that meeting from open -> waiting using rules:
       - Only for per-individual drafts (recipientEmail or recipientName present)
       - Match action items where ownerEmail == recipientEmail OR ownerName == recipientName
       - Only update items with status == open
   - Implement this server-side inside the draft creation endpoints so it always happens.
3) Add UI in Inbox and Meeting detail:
   - Quick actions: “Mark waiting”, “Mark done”
   - For waiting items: “Mark done” should be one click.
4) Add a small “Follow-up status” indicator in Draft detail:
   - generated / exported

Acceptance:
- Generate drafts -> create Gmail/Outlook draft -> matching actions move to Waiting automatically.

========================
G) INVITE ACCEPTANCE UI (TOKEN FLOW)
========================
Server already has:
- GET /api/invites/:token
- POST /api/invites/:token/accept (currently requires userId in body)

Fix and add UI:
1) Update POST /api/invites/:token/accept to use req.session.userId (authenticated user) rather than requiring userId in body.
2) Add client route/page: /invite/:token
   - If not logged in: redirect to login then back to invite
   - If logged in: show workspace name + role + accept button
   - On accept: refresh workspaces list, setCurrentWorkspace(workspaceId), navigate to /inbox
3) Add a “Pending invites” section in Workspaces screen (optional if time allows).

Acceptance:
- Clicking invite link joins workspace and sets it active.

========================
H) OCR IMPORT: ADD “TAKE PHOTO” FOR MOBILE + KEEP UPLOAD FOR WEB
========================
You already have OCR upload. Make it feel native on phones.

Implement:
1) Add @capacitor/camera dependency and integrate camera capture when running in Capacitor.
2) In Capture screen “Import handwritten notes”:
   - Web: Upload photo -> POST /api/ocr
   - Native: Take photo -> upload image blob/base64 -> POST /api/ocr
3) Keep privacy defaults:
   - Do not store image unless user.allowImageStorage is true AND user explicitly opts-in.
   - Store only extracted text in meeting notes.
4) Improve failure states: low quality guidance, retry, discard.

Acceptance:
- On mobile wrapper: Take photo -> text appears -> insert -> extract.

========================
I) AGENDA VIEW (NOT FULL CALENDAR)
========================
Implement /agenda as a lightweight “what’s due” view:
- Overdue, Today, Tomorrow, Next 7 Days
- Data source: actionItems.dueDate in current workspace + open/waiting statuses
- Link it from Inbox header.

Acceptance:
- Agenda reflects current workspace actions and feels like a “daily plan”.

========================
J) TESTS: ADD REAL “HAPPY PATH” E2E THAT MATCHES PRODUCT SPINE
========================
Update Playwright tests so they reflect real flows and don’t depend on OAuth or AI keys:
1) Update existing smoke test to use valid login credentials (email+password) OR create a deterministic demo user if missing.
2) Add an E2E test:
   - Login
   - Create meeting
   - Paste notes
   - Extract (Mock AI if keys missing)
   - Verify Inbox shows open actions
   - Generate drafts
   - Verify drafts appear
   - (If integrations disabled) verify “Create Gmail Draft” is disabled with “Not configured” message
3) Ensure tests pass with AI disabled and integrations disabled.

========================
DELIVERABLE
========================
After changes:
- No “pending” status remains.
- reminderAt works end-to-end.
- Onboarding/settings persist to server.
- Default “My Work” workspace exists and is auto-selected.
- Personal is cleanly separated.
- Draft export moves actions to waiting.
- Invite acceptance works via /invite/:token.
- Mobile camera OCR works (Capacitor).
- Agenda view exists.
- Playwright tests pass in Mock mode.

IMPORTANT: keep changes minimal and consistent with the existing style/components; do not redesign UI unnecessarily.
